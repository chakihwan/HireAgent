# M5 체크리스트 + 향후 로드맵

> 작성일: 2026-05-25
> 상태: M1~M4+ 완료, M5 진행 중 (본인 실사용 + 피드백 반영)

---

## 🔥 1순위 — 본인 실사용 (테스트 가능해지면 즉시)

- [ ] `/projects`에 본인 GitHub URL 모두 인덱싱
  - 메인 프로젝트 5-10개 repo
- 레포 단 1 개 올리고 확인했을때는 할루시네이션이 너무너무너무너무너무 심함. 제조공정에서 수집된 데이터 분석 레포만 올렸는데 ML 모델 프로젝트였지만, 딥러닝 경험을 바탕으로 **[참고 경험 1]**에서 DNN 모델 정확도를 **95% 이상 향상**시켰고, **[참고 경험 2]**에서 실시간 센서 데이터 분석을 통한 CNC 불량 예측 AI 모델 개발로 성과를 냈습니다. 성능 개선에 주력했습니다.

**협업 경험**

중소벤처기업부, KAIST, UNIST와의 **[참고 경험 3]** 프로젝트를 통해 다양한 팀과 협업하며 성장했습니다.<-이런식으로 답변함
  - README + docs 자동 수집 확인
- 레포 주소로 올렸는데 리드미 참조 되는건지... 일단 
- [ ] 본인 이력서 PDF 업로드 → 텍스트 추출 확인
- 이력서 참조는 잘하는데, 너무 뒤죽박죽임. 예를 들면 학교에서 한 프로젝트랑 회사에서 한 프로젝트랑 다 섞여있음. 사람인 이력서 양식만 테스트해봄. 
- [ ] 기존 자소서 (있다면) 텍스트로 업로드 → `source_type=essay`
- 지금 내 데이터 > 데이터 추가 너무 UI/UX상 불친절해.. 어떻게 올려야할지 감도 안옴
- [ ] 실제 채용 공고 3-5개로 자소서 생성
  - 텍스트 붙여넣기 / URL 페칭 / 자유 입력 등 다양한 입력 방식 검증
- 텍스트 읽어서 회사나 직무 파악은 굿, URL 감지되면 자동으로 크롤링? 하는것도 확인은 됨
  - RAG 인용된 경험이 적절한지 확인
- RAG 인용이 현재는 레포 하나, 사람인 이력서 양식 하나 이렇게 테스트 해봤을때는 너무 뒤죽박죽이야. 참조하는지도 잘 모를정도로
- [ ] 생성 품질 평가 → 부족한 부분 메모 (`docs/feedback.md` 신규 작성)
- 이 문서에다가 작성하라고?
- [ ] 발견된 UX/버그 이슈 목록화 → 본 체크리스트에 추가
- 현재까지 발견한건 새로고침해도 그 페이지에 머무는거? 사용자 경함 측면에서 너무너무너 페이지가 불친절하다..? UX 관점에서 많이 생각해주면 좋겠는데 이건 아마 UI 고도화 할때 진행하겠지 ? 너가 ? 

---

## ⭐ 2순위 — 보안 + 미완 기능 완성 (M5 후반)

### 보안
- [ ] **API 키 DB 암호화 저장** (CLAUDE.md Rule #2 미완)
  - `app/utils/crypto.py` (Fernet) 이미 있음, 라우터 미연결
  - `UserLLMConfig` 테이블 활용 (`encrypted_keys` JSONB)
  - 현재는 localStorage만 사용 → SaaS 배포 시 필수
- [ ] `.env`에 `ENCRYPTION_KEY` 강제 검증 (default 값일 때 startup 에러)
- [ ] CORS 화이트리스트 정리 (배포 시 도메인 한정)

### 인프라
- [ ] 임베딩 모델 캐시 볼륨 마운트
  - `docker-compose.yml`에 `~/.cache/huggingface` mount
  - 컨테이너 재시작 시 KURE-v1 재다운로드 방지
- [ ] 백엔드 GPU passthrough 검토 (KURE-v1 5배 가속)

### 미완 기능 (요구사항 §4.1)
- [ ] F-3.3: 공백 제외 글자수 옵션 (현재 공백 포함만)
- [ ] F-5.3: 프로바이더별 모델 목록 동적 로딩 (현재 하드코딩)
- [ ] F-5.6: 토큰 사용량 누적 표시
- [ ] F-7.1: 프로젝트 카드 자동 요약 (Phase 2 영역이긴 함)

### 품질
- [ ] `backend/tests/` 디렉토리 + pytest 셋업
  - API smoke test (jobs/library/projects CRUD)
  - RAG 검색 (인덱싱 → 검색 결과 distance)
  - 글자수 검증 함수 단위 테스트
- [ ] structlog 도입 (구조화된 로깅)
- [ ] 백엔드 헬스체크 강화 (DB 연결 + Ollama 응답 + KURE-v1 로드 상태)

---

## 🎨 3순위 — UI 고도화 (실사용 후 우선순위 재조정)

### 글로벌 UX
- [ ] **대시보드 홈 페이지** (`/`)
  - 현재 `/` → `/generate` 즉시 리다이렉트라 진입점이 약함
  - 통계 카드 (지원 N개 / 자소서 N개 / 인덱싱 N개 / 합격률)
  - 최근 활동 타임라인 (최근 생성 자소서, 상태 변경 등)
  - 빠른 시작 버튼 (새 자소서 / 새 지원)
- [ ] **다크 모드** (`next-themes` + Tailwind dark:)
- [ ] **Toast 알림 통일** (현재 `alert()` + emerald 메시지 혼재 → 일관된 toast)
- [ ] **로딩 스켈레톤** 컴포넌트 통일
- [ ] **한국어 폰트** (Pretendard / Suit) — 현재 Geist는 영어 폰트
- [ ] **모바일 반응형 점검** (현재 max-w-3xl 위주, 모바일에서 동작 확인 안 됨)
- [ ] **빈 상태 일러스트** (현재 텍스트만)

### `/generate` 페이지
- [ ] **항목별 톤/페르소나 다르게 설정** (현재 전체 공통)
  - 자기소개는 친근, 직무경험은 공식적 등
- [ ] **항목 순서 드래그** (`@dnd-kit/sortable`)
- [ ] **결과 인라인 편집** (현재 readonly textarea — 편집해서 라이브러리 저장)
- [ ] **미세 조정 버튼** ("좀 더 도전적으로", "수치 강조", "더 짧게")
  - 결과 카드별 1-click 재생성
- [ ] **결과에 RAG 인용 펼치기** ("어떤 경험을 참고했는지" 토글)
  - 멀티에이전트/RAG 강점을 시각적으로 드러내기
- [ ] **생성 취소 버튼** (SSE 도중 abort)
- [ ] **A/B 비교 모드** (같은 항목 2번 생성 → 나란히 비교)
- [ ] **지원 연결 드롭다운** (저장 시 application_id 선택)
- [ ] **JD 분석 결과 미리보기** (요구역량/인재상 추출 단계 결과 표시)

### `/library` 페이지
- [ ] **검색** (제목/내용 풀텍스트)
- [ ] **통계 헤더** (총 N개 · 최종 N개 · 합격 회사 N개)
- [ ] **같은 카테고리 버전 비교** (diff 뷰)
- [ ] **합격 자소서 모아보기** (status=passed_* 조인 필터)
- [ ] **태그 추가** (메타 자유 태그 — JSONB로 확장)
- [ ] **TXT/DOCX export** (최종본 일괄 다운로드)

### `/jobs` 페이지
- [ ] **칸반 보드 뷰** (상태별 column — draft / submitted / passed_* / rejected)
  - 카드 드래그로 상태 변경
- [ ] **D-Day 배지** (deadline까지 N일)
- [ ] **캘린더 뷰** (마감일 시각화)
- [ ] **합격률 통계** (서류/면접/최종 단계별 %)
- [ ] **진행 단계 타임라인** (지원→서류→면접→최종)
- [ ] **회사별 자소서 카운트** (몇 개 항목 작성했는지)

### `/projects` 페이지
- [ ] **GitHub 인덱싱 진행률** (파일별 진행 표시 — SSE 또는 polling)
- [ ] **청크 미리보기 모달** (목록에서 클릭 시 전체 내용 보기)
- [ ] **검색 결과 하이라이팅** (매칭 키워드 강조)
- [ ] **메타데이터 일괄 편집** (여러 청크 선택 → 카테고리 변경)
- [ ] **임베딩 분포 시각화** (UMAP 2D plot, 선택)
- [ ] **재인덱싱 옵션** (같은 project_name 기존 청크 자동 삭제 후 새로 인덱싱)

### `/settings` 페이지
- [ ] **프리셋 적용** ("성능 우선" / "비용 우선" / "오프라인" 프리셋 한 번에)
  - 성능: 작성=Opus, 평가=Sonnet, 압축=Haiku
  - 비용: 모두 Haiku/Mini
  - 오프라인: 모두 Ollama exaone
- [ ] **테스트 호출 버튼** (현재 설정으로 짧은 호출 → 응답 시간/성공 표시)
- [ ] **토큰 사용량 누적** (세션별/일별)
- [ ] **모델별 추정 비용 표시** (입력/출력 토큰 단가)

### 새 페이지 후보
- [ ] `/dashboard` — 홈 대시보드 (위 항목)
- [ ] `/insights` — 자소서 패턴 분석 (자주 쓰는 단어, 평균 점수 트렌드)
- [ ] `/templates` — 항목 조합 템플릿 ("개발자 1년차 묶음" 등)
- [ ] `/help` — 사용 가이드 + FAQ

---

## 💎 중기 (M6 ~ Phase 2)

### 확장 기능 (요구사항 §4.2)
- [ ] F-8.1: 면접 코치 (예상 질문 + 답변 가이드)
- [ ] F-8.2: 매칭 분석 (공고-이력서 임베딩 유사도 점수)
- [ ] F-8.3: 회사 분석 (외부 데이터 보강)
- [ ] F-8.4: 노드 그래프 UI (React Flow, Dify 벤치마킹)
- [ ] F-8.5: 커리어 타임라인 (인덱싱된 경험 시간순)
- [ ] F-8.6: 브라우저 확장프로그램 (사람인/잡코리아 드래그 금지 우회)

### 배포 (Phase 3)
- [ ] OAuth 인증 (Google/GitHub)
- [ ] 사용자 가입/로그인 페이지
- [ ] 멀티테넌시 검증 (user_id 격리 실측)
- [ ] Railway/Fly.io 백엔드 배포
- [ ] Vercel 프론트 배포
- [ ] HTTPS + 도메인
- [ ] 백엔드 별도 임베딩 서버 분리 (또는 외부 API 전환)

---

## 📚 문서 + 포트폴리오

- [ ] **루트 `README.md` 강화**
  - 현재 docs/README.md는 풍부하지만 GitHub 첫 페이지(루트 README)는 미흡
  - 메인 데모 GIF/스크린샷
  - "What it does / Why / How to run" 임팩트 있게
  - 기술 스택 배지 + ADR 링크
- [ ] **데모 시나리오** (`docs/demo.md`)
  - 첫 사용 5분 가이드 (GitHub 인덱싱 → 공고 입력 → 자소서 생성)
  - 각 단계 스크린샷
- [ ] **배포 가이드** (`docs/deployment.md`)
  - Railway + Vercel 단계별
- [ ] **API 사용 가이드** (`docs/api.md`)
  - FastAPI `/docs` 자동 생성이 있지만, 핵심 흐름 narrative

---

## 🔧 기술 부채

- [x] essay_writer 출력에 `**bold**` 마크다운 남아있는 케이스 정리 (`_clean_output` 보강)
  - `app/utils/text_cleaner.py`로 분리 (essay_writer + compressor 공통 사용)
  - compressor도 `_clean_output` 미적용 → `clean_llm_output()` 추가 (2026-05-26)
  - RAG 출처 레이블 `[참고 경험 N]` 누출 방지 정규식 추가
  - 볼드/이탤릭/불릿/글자수메타 줄 제거
- [ ] orchestrator의 `_pack_draft` 제거 흔적 검증
- [ ] 에러 핸들링 일관성 (`raise HTTPException` vs ValueError vs Exception)
- [ ] 프론트 API 에러 메시지 한국어 통일
- [ ] CORS 설정 환경별 분기 (`config.py`에서 origin 리스트 관리)
- [ ] Alembic 마이그레이션 다음 단계 (현재 1개 — 새 컬럼 추가 시 절차 문서화)

---

## 사용 방법

- 이 문서는 **살아있는 체크리스트**. 작업 시작 시 `- [ ]` → `- [x]` 갱신
- 새 아이디어/이슈 발견 시 적절한 섹션에 추가
- 우선순위 변경 시 자유롭게 이동 (1순위 → 3순위 등)
- 완료된 큰 단위는 `CHANGELOG.md`로 이주 후 본 문서에서 제거

---

## 변경 이력

| 날짜 | 변경 |
|------|------|
| 2026-05-25 | 최초 작성 (M5 시작 시점, M4+ GitHub/파일 업로드 완료 후) |
| 2026-05-26 | 기술부채 수정: `clean_llm_output` 공통 유틸 분리, compressor/essay_writer 마크다운 클린업, `[참고 경험 N]` 누출 방지 |
